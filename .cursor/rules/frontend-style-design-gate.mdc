---
description: 前端样式/视觉改动前先经 UI 设计技能评估，无更优方案再按用户要求实现
alwaysApply: true
---

# 前端样式设计门禁

## 何时触发

用户提出的改动**触及前端样式或视觉呈现**时触发，包括但不限于：

- 布局、间距、配色、字体、动效、主题
- 组件外观、3D/Canvas 场景视觉、图标与插画风格
- 「改成 XX 样」「更好看」「不要 XX 效果」等视觉导向需求

**不触发**（直接按用户要求做）：

- 纯逻辑、API、数据、脚本，不涉及 UI 呈现
- 用户已明确选定方案（如「就用 A，不要 B」）且无需再比选
- 机械性修正（指定色值、像素、类名替换等，无设计决策空间）

## 执行顺序（硬规则）

1. **先读 UI 设计技能**（按场景选用，可组合）：
   - `frontend-design` — 美学方向、排版、差异化视觉
   - `brainstorming` — 新视觉功能或行为变更前的意图对齐
   - `visual-companion`（brainstorming 子流程）— 需要并排对比 mockup 时
2. **内部快速比选**：用户方案 vs 1～2 个更优或更稳的呈现方案（一句话说明各方案差异与 trade-off）。
3. **决策**：
   - **有更优方案**：先用 2～4 句话向用户说明推荐项及理由，等确认后再实现；不要静默偏离用户原话。
   - **无明确更优方案**：按用户要求实现，不强行加戏。

## 输出要求

- 比选过程可在思考中完成；给用户看的只需结论，不贴长设计文档。
- 实现时仍遵守：最小 diff、复用现有组件与 token、不擅自扩 scope。
- 样式改动**不进源码导读**（见 `source-guide-sync` 规则）。

## 示例

用户：「关系网背景不要马赛克模糊。」

- ✅ 先想：纯色深空 / 轻渐变 / 稀疏星点底纹等，再选最贴合「亮星关系网」且干净的方案；若无争议则直接做纯色。
- ❌ 未评估就堆回 shader 噪点，或擅自改成与用户场景无关的「模板暗色+acid 绿」风。
