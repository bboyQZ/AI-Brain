---
description: 效果优先且零付费；改动大小不挡路，但性能须可接受、绝不推荐花钱方案
alwaysApply: true
---

# 效果优先（零付费 · 性能可接受）

## 核心原则

用户描述的是**想要的样子 / 最终效果**时，AI 在**方案设计、技术选型、架构决策**阶段：

- **不得**把「改动小」「diff 少」「沿用现有实现」当作优先理由
- **必须**优先选**在硬约束内**最能稳定达成终态的路径，哪怕需要换栈、重写、引引擎、拆模块、大迁移

「改动大小」只用于**分期交付说明**，不能用来否决更优方案。

## 硬约束（不可违反）

### 1. 绝不花钱

**禁止**推荐或默认采用任何需要**付费**才能完成的方案，包括但不限于：

- 付费模型 / 贴图 / 材质包 / 角色库 / 插件授权
- 按量计费 API、云渲染、Pixel Streaming、托管 3D 服务等**会产生账单**的服务
- 需订阅或一次性购买的 SaaS / 素材站会员（除非用户**明确**已有授权且指定使用）

**允许**：开源、CC0、免费个人版引擎、自研 Blender 资产、项目内已有免费资源。

方案中若某能力只有付费途径，必须**直说**并改推**免费替代**（自制、开源、降级表现），不得静默假设「可以买」。

### 2. 性能别太糟

目标环境：**普通笔记本核显 / 中端浏览器**，公司大楼类场景应**可流畅漫游**（非电影级但不可用）。

方案须主动考虑：

- 模型体量（GLTF 压缩、按翼楼拆分、LOD、实例化）
- 贴图尺寸与数量（合理上限，避免 4K 滥铺）
- 灯光与后期（实时灯光数量、SSAO 等可选、可关）
- 首屏加载（分块加载、占位 → 渐进替换）

**禁止**为追求终态默认采用明显超重方案（如整楼单文件数十 MB 无 LOD、全场景实时光追级配置），除非附带**免费**的降载策略。

性能不必极致优化，但须达到「**能正常用、不卡到难受**」；若终态与性能冲突，**明说 trade-off** 并给免费降载版。

## 方案产出顺序

1. **终态**：用户验收时看到/感受到什么
2. **硬约束自检**：是否零付费、性能是否可接受；不满足则换路径
3. **推荐路径**：技术 / 免费资产来源 / 架构（R3F、Babylon、Blender→GLTF 等）
4. **对比维度**：能否达终态、零付费、性能风险、周期——**不用「改动行数」当主维度**

## 与「最小改动」的关系

- **方案阶段**：效果 + 零付费 + 性能可接受 > 改动小
- **落地阶段**：在已定路线上仍避免无关文件扩散

## 示例

用户：「公司大楼要真实，引擎也行。」

- ❌ 「买 Sketchfab 付费办公楼模型 + 云渲染。」
- ❌ 「继续手搓方块，改动最小。」
- ✅ 「终态：可漫游园区。Blender 自建 + CC0 家具 → GLTF + Draco；R3F + 免费 HDRI；角色 Vroid 免费导出或自建模。按翼楼分块加载，核显目标 30fps 级。」

用户：「要最高画质，钱无所谓。」

- 仍按**零付费**硬约束执行，除非用户**当条消息明确**解除预算限制。
