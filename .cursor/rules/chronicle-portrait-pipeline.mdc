---
description: 人物纪事肖像新增/更换后必须跑 optimize + enhance 管线
globs: frontend/public/chronicle/portraits/**,frontend/src/chronicle/flows.ts,frontend/scripts/*portrait*
alwaysApply: false
---

# 人物纪事肖像管线

触及 `frontend/public/chronicle/portraits/` 原图或 `flows.ts` 的 `PORTRAIT_BY_ID` 时，必须完成分级资源生成。

## 必做步骤

1. **原图落盘**：放进 `frontend/public/chronicle/portraits/`（仅根目录，不要放进 thumbs/display/enhanced）
2. **挂载路径**：在 `frontend/src/chronicle/flows.ts` 的 `PORTRAIT_BY_ID` 增加节点映射
3. **跑管线**（在 `frontend/` 目录）：

```powershell
Set-Location "D:\develop\AI-Brain\frontend"
npm run portraits-pipeline
```

等价于依次执行 `optimize-portraits`（display/thumbs WebP）与 `enhance-portraits`（enhanced/ 2× 锐化 WebP）。

## 产物与前端用法

| 目录 | 用途 |
|------|------|
| 根目录原图 | 全屏点「100%」原图 1:1 |
| `display/` | 底部浮层预览 |
| `thumbs/` | 缩略图条 |
| `enhanced/` | 全屏画廊默认（放大更清晰） |

未生成子目录时界面会回退原图，但**新增照片后必须跑管线**，不要只提交 JPG。

## 可选：Real-ESRGAN AI 超分

将 [realesrgan-ncnn-vulkan](https://github.com/xinntao/Real-ESRGAN/releases) Windows 版解压到：

`tools/realesrgan-ncnn-vulkan/realesrgan-ncnn-vulkan.exe`

再跑 `npm run enhance-portraits` 会优先用 AI；否则用 sharp 2× Lanczos + 锐化。

## 禁止

- **调用外部 API / 云端服务**做增强（必须本地 `npm run portraits-pipeline` 或本地 Real-ESRGAN 可执行文件）
- 只加原图不跑 `portraits-pipeline`
- 把 thumbs/display/enhanced 当原图目录维护
- 用压缩糊图当「原图」却不说明（应换更高清源文件）
